spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/eshop
    username: postgres
    password: myP0stgres

  flyway:
    baseline-on-migrate: true