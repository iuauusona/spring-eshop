spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:~/test;AUTO_SERVER=TRUE

server:
  hostname: localhost
  port: "${PORT:8080}"
mail:
  server:
    host: smtp.gmail.com
    port: 587
    username: iuauusona01@gmail.com
    password: eajy lbkl pklj azrd